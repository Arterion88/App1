<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App1"
             x:Class="App1.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label x:Name="lblTitle" 
               Text="Vítejte"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand" />
        <Label x:Name="lblTitle2" 
               Text="Nikdo vám nedá více..."
               HorizontalOptions="Center"
               VerticalOptions="End" />
        <ScrollView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <StackLayout x:Name="parent"
                         HorizontalOptions="CenterAndExpand"
                         VerticalOptions="FillAndExpand"/>
        </ScrollView>
        <StackLayout HorizontalOptions="FillAndExpand">
            <Grid x:Name="gridPass" Margin="30,0,30,0">
                <Label Text="Napište kód:" Grid.Column="0" HorizontalOptions="Center" VerticalTextAlignment="Center" HorizontalTextAlignment="End"/>
                <Entry x:Name="editPass" Grid.Column="1"/>
            </Grid>
        </StackLayout>

        <StackLayout Orientation="Horizontal">
            <Button Text="QR Kód" x:Name="btnQR" Clicked="BtnQR_Clicked"/>
            <Button Text="Další"  x:Name="btnCode" Clicked="BtnCode_Clicked" VerticalOptions="End" HorizontalOptions="FillAndExpand"/>
        </StackLayout>
        <Button Text="Poslat výsledek" x:Name="btnSubmit" Clicked="BtnSubmit_Clicked"/>

    </StackLayout>

</ContentPage>
